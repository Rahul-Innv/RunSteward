# Normalize line endings: store text as LF in the repo, check out native on each OS.
* text=auto eol=lf

# Windows batch/PowerShell scripts want CRLF if any are ever added.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
