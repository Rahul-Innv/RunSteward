<!-- Select "Bug" from the Description template dropdown when opening an issue. -->
/label ~bug

**What happened?**
A clear description.

**To reproduce**
Steps / the command you ran.

**Expected behavior**
What you expected instead.

**Environment**
- OS:
- Node.js version:
- Claude Code version:
- `mode` / `dry_run` from your config:

**Logs / output**
Paste relevant lines from `decisions.log` or `node bin/status.js`.
⚠️ Do NOT paste secrets or the contents of your `~/.claude/.credentials.json`.
