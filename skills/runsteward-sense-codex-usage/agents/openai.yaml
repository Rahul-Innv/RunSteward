interface:
  display_name: "RunSteward Sense Codex Usage"
  short_description: "Emit one typed Codex usage-evidence envelope with bounded red..."
  default_prompt: "Use $runsteward-sense-codex-usage to emit one typed Codex usage-evidence envelope with bounded redacted references."
policy:
  allow_implicit_invocation: false
