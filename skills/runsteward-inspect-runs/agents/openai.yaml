interface:
  display_name: "RunSteward Inspect Runs"
  short_description: "Return one bounded read-only inspection of existing queue or..."
  default_prompt: "Use $runsteward-inspect-runs to return one bounded read-only inspection of existing queue or run evidence."
policy:
  allow_implicit_invocation: false
