interface:
  display_name: "RunSteward Stop Run"
  short_description: "Produce one evidence-bound stopped transition that remains el..."
  default_prompt: "Use $runsteward-stop-run to produce one evidence-bound stopped transition that remains eligible only for a separately checked resume."
policy:
  allow_implicit_invocation: false
